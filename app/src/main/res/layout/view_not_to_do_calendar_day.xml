<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="kr.co.nottodo.view.monthcalendar.DateType" />

        <variable
            name="day_item"
            type="kr.co.nottodo.view.monthcalendar.NotToDoCalendarDay.Day" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:padding="18dp">

        <!-- TODO font 및 텍스트 스타일 만들어서 적용해야 합니다 -->
        <TextView
            android:id="@+id/tv_day_name"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:gravity="center"
            android:includeFontPadding="false"
            android:text="@{day_item.label}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:setCalendarDayTextColor="@{day_item.state}"
            tools:text="31" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>